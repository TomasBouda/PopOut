<Window x:Class="YouPipe.Player.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:vlc="clr-namespace:Vlc.DotNet.Wpf;assembly=Vlc.DotNet.Wpf"
		mc:Ignorable="d"
		Title="{Binding MyTitle}" 
		WindowStyle="None"
		WindowStartupLocation="CenterScreen"
		Height="306.393" Width="449.262"
		>
	<Grid >
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="20" />
			<RowDefinition Height="1*" />
		</Grid.RowDefinitions>

		<TextBox Name="txtVideoUrl"
			PreviewMouseDown="TextBox_MouseDown" 
			KeyUp="TextBox_KeyUp"
			Grid.ColumnSpan="2" 
			TextWrapping="Wrap"
			Background="Black" 
			Foreground="White" 
			BorderBrush="Black" MouseEnter="txtVideoUrl_MouseEnter" MouseLeave="txtVideoUrl_MouseLeave" 
			/>
		
		<vlc:VlcControl Grid.Row="2" x:Name="vlcPlayer" />

		<Grid  Grid.Row="2" Name="grdPlayList" Width="200" Grid.Column="2" Visibility="Visible">
			<ListBox  ItemsSource="{Binding}" MouseEnter="ListBox_MouseEnter">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<TextBlock Text="{Binding Title}"/>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Grid>

	</Grid>
</Window>
